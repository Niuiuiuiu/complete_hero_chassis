# Changelog

## v0.8 — 代码重构 & 加重功率测试 (2026-06-28)

### 功率控制测试
- **加重底盘全系统功率测试**: 底盘加装全部负载（M3508 轮速电机 + DM4340 关节电机），实测功率控制效果
  - 稳定控制在 100W 以下，基本不超功率
  - 机动性略微下降：猛推摇杆时功率削减介入更快更激进，加速感稍弱
  - 权衡选择：优先保证功率安全，牺牲部分机动性

### 重构
- **回调函数集中管理** (`app/callback.c`)
  - 所有 HAL `__weak` 回调统一迁移至 `callback.c`：`HAL_GPIO_EXTI_Callback`、`HAL_TIM_PeriodElapsedCallback`、`HAL_CAN_RxFifo0MsgPendingCallback`、`HAL_CAN_RxFifo1MsgPendingCallback`
  - `main.c` 精简 176 行，`drv_can.c` 精简 90 行
  - `drv_can.c` 专注 CAN 工具函数（发送、滤波配置）
- **IMU 初始化封装**: `BMI088_Full_Init` 移入 `imu_data.c`
  - 整合 `BMI088_Init`、`BMI088_Start`、零偏校准、Mahony 对准、`TIM14` 启动
  - 回调用 `imu1.imu_ready` 取代全局 `imu_ready` 标志位

---

## v0.7 — 全系统功率控制 (2026-06-27)

### 功率控制策略
- **控制范围**: 底盘 M3508 轮速电机 + DM4340 关节电机统一纳入功率预算
  - M3508 电流受 `cap_factor` 直接削减
  - 功率上限收紧至 60W，为 DM4340 腿关节留出余量
- **效果**:
  - 主动悬挂模式：稳定在 100W 以下
  - 上台阶模式：瞬时形成约 162W 尖峰后迅速回落到 100W 以下
  - 尖峰原因：上台阶时 DM4340 瞬间满力矩出力，低通滤波延迟导致 PID 滞后约 5~10ms

### 变更
- **PID 参数微调**: 功率上限 70→60W，Ki 0.08→0.09
- **M3508 速度环 Kp**: 3.0→2.8，进一步降低电流冲击
- **函数重命名**: `power_meter_init` → `power_model_init`，语义更准确

---

## v0.6 — 功率控制策略收敛 (2026-06-26)

### 功率控制策略
- **控制范围**: 仅对底盘 M3508 轮速电机做功率限制，DM4340 腿关节不受影响
- **效果**: 底盘电机功率可稳定控制在 100W 以下

### 变更
- **PID 参数重新整定**: 功率上限 100W，Kp 0.5→0.25，Ki 0.01→0.08
  - P 项响应温和，不一步砍死；I 项持续出力维持削减
  - 恢复 `power_output_clamp` 累加模式 (`+=`)，存储值正确限幅
- **M3508 速度环 Kp 调低**: 4.0→3.0，减小大电流冲击
- **M3508 速度环积分限幅收紧**: `v_error_sig` 限幅 ±170000（原 ±800000）
  - 功率削减时速度跟不上目标，收紧积分上限防止恢复瞬间电流回弹

---

## v0.5 — 功率模型框架 & PID 参数调优 (2026-06-25)

### 新增
- **功率模型框架** (`app/power_ctrl.h`)
  - `power_model_t` 结构体，包含电流/转速/真实功率指针 + k0~k5 多元多项式系数
  - `power_meter_init`: 绑定功率计和 M3508 电机数据源
  - `set_power_model_para`: 设定多项式系数
  - `calc_power_model`: 基于电流和转速的二次多项式功率估计

### 修复
- **`power_output_clamp` 累加 bug**: `+=` 改为 `=`，不再持续累加导致输出跑到 20000+
  - 每轮独立计算系数，功率恢复后立即回升到 1.0

### 变更
- **串口输出恢复**: VOFA 数据发送取消注释，`HAL_Delay` 50→25ms
- **功率 PID 参数**: Kp 0.2→0.5，限幅下限 0.3→0.0
  - 响应更快，功率超标时削减更彻底

---

## v0.4 — 功率控制 PID (2026-06-19)

### 新增
- **功率控制模块** (`app/power_ctrl.c/.h`)
  - 增量式 PID，输入底盘实时功率 `P_x1W_chassis`，输出电流削减系数
  - 功率计未就绪时跳过计算，避免上电误削
  - `power_output_clamp` 对累计系数限幅 [0.3, 1.0]，防积分饱和

### 变更
- **`drv/M3508.h/.c`**: `M3508_currentsend` 新增 `Limiting_Coefficient` 参数
  - 四个轮子电流统一乘削减系数后再发 CAN
- **`main.c`**: TIM13 中断内调用功率 PID，削减系数传入 `M3508_currentsend`
  - 功率上限 100W，Kp=0.2，Ki=0.01
  - 主循环 `HAL_Delay` 1ms → 50ms，降低阻塞频率
- **落腿参数微调**: DM43402/43403 落腿目标位置 ±1.0 → ±0.4

---

## v0.3 — 稳定性修复 (2026-06-14)

### 修复
- **关闭 TIM10 中断**: 移除 NVIC 使能和 ISR 函数，消除 84kHz 无用中断
  - TIM10 仅用于 IMU 加热 PWM，不需要中断
  - 大幅减少中断负载（~88kHz → ~4kHz），debug 全速模式不再卡顿
- **IMU 上电保护**: 新增 `imu_ready` 标志位，EXTI 回调在 IMU 初始化完成前不处理中断
  - 解决换板子上电时 IMU 提前触发中断导致飞车的问题

### 变更
- **主循环串口发送暂时注释**: VOFA 数据输出暂停
  - 阻塞式 `HAL_UART_Transmit` 在 VOFA 连接时与 debug 探针抢总线
  - 后续可尝试延长 `HAL_Delay` 降低发送频率，或改用 DMA 发送，争取同时 debug 和看 VOFA

---

## v0.2 — 功率计驱动 & 串口输出 (2026-06-12)

### 新增
- **功率计驱动** (`drv/power_meter.c/.h`)
  - 支持吴张扬 (WZY) 和欧阳云翔 (OUYANG) 两种功率计
  - CAN ID `0x212` 接收，解析电池电压/电流/底盘实时功率
  - 低通滤波 (`alpha=0.1`) 平滑功率数据
  - 支持设定功率上限、目标功率并回传

### 变更
- **`main.c`**: 主循环 USART6 输出从 IMU 姿态数据切换为底盘功率 `P_x1W_chassis`
  - `power_send[0]` = 底盘实时功率 (W)
  - 发送格式: 3×float + tail `{0x00,0x00,0x80,0x7f}` = 16 字节/帧
- **`drv_can.c`**: RxFifo1 回调新增 `POWERMETER_CAN_RX_ID` 分发
- **CAN 滤波掩码**: `0x7F0` → `0x700`（收紧单 ID 匹配精度）

### 清理
- `.gitignore` 新增排除编译产物目录 (`MDK-ARM/complete_hero_chassis/`, `MDK-ARM/RTE/`)

---

## v0.1 — 初始版本 (2026-06-12)

- STM32F407 英雄机器人底盘控制系统
- M3508 麦克纳姆轮底盘 (CAN 总线, 速度/位置双环 PID)
- DM4340 四腿关节 MIT 模式力控
- BMI088 IMU 姿态解算 (Mahony 互补滤波)
- DBUS 遥控接收, 云台跟随模式
- 抬腿状态机 (前/后腿依次抬放)
